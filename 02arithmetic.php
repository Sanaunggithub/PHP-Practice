<?php
    // arithmetic operator
    // + - * / ** %

    // increment/ decrement operator
    // ++, --

    // operator precedence
    // ()
    // ** 
    // * / %
    // + -

    $x = 10;
    $y = 2;
    $z = null;

    $z = $x + $y;
    $z = $x - $y;
    $z = $x * $y;
    $z = $x / $y;

    $z = $x ** $y;


    $counter = 0;
    $counter++;
    $counter++;

    $counter+=2;
    $counter-=3;
    
    echo $counter;
?>