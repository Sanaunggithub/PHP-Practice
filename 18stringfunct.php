<?php
    $username = "San Aung";
    $phone = "123-456-7890";
    // $username = strtolower($username);
    // $username = strtoupper($username);
    // $username = trim($username);
    // $username = str_pad($username, 20, "$");
    // $phone = str_replace("-", "/",$phone);
    // $username = strrev($username);
    // $username = str_shuffle($username);
    // $username = strcmp($username, "San Aung"); return 0 if same, -1 if different
    // $count = strlen($phone);
    // $index = strpos($username, " ");
    // $firstName = substr($username, 0, 3); 
    // $lastName = substr($username, 4);

    // $fullName = explode(" ", $username);  split string into an array

    // $username = array("San", "Aung");
    // $username = implode("-", $username); change array into one line string

    echo $username;
?>